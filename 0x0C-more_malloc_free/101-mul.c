#include <stdlib.h>
#include <stdio.h>

/**
 * main - multiplies 2 numbers
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int total, num1, num2, i;

	if (argc != 3)
	{
		printf("Error\n");
		exit(98);
	}

	return (0);
}
