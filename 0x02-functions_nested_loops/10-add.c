#include "main.h"
/**
 * add - add two integers
 * @int: data type of the parameters
 * Return: result of add
 */
int add(int, int)
{
	int num1, num2;
	int total;

	total = num1 + num2;

	return (total);
}
